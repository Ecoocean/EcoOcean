import chroma from "chroma-js";

export const redGreenScaleBoxes = chroma.scale(['#EDF7ED', '#FFF4E5', '#FDEDED']).domain([0, 100]);
export const redGreenScaleLayers = chroma.scale(['#b2ff59', '#ff9800', '#ff3d00']).domain([0, 100]);

